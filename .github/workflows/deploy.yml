name: Auto Deploy to Production

on:
  push:
    branches: [ "master" ]
    paths:
      - ".github/workflows/deploy.yml"
      - "setup-runner.sh"
      - "enable-auto-deploy.sh"
      - "bhl-oms/**"
  workflow_dispatch:

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy on Mac mini
    runs-on: [self-hosted, macOS, production]
    timeout-minutes: 30

    env:
      PATH: /opt/homebrew/bin:/opt/homebrew/sbin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin
      PROJECT_DIR: bhl-oms

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Trust checked out workspace
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Copy secret keys (not in git)
        run: cp -r /Users/tungtranle/Projects/Beer-HL/$PROJECT_DIR/keys $PROJECT_DIR/keys

      - name: Debug Docker access
        run: |
          echo "PATH = $PATH"
          which docker || echo "docker not found"
          docker info 2>&1 | head -5 || echo "docker info failed"
          docker compose version || echo "docker compose failed"

      - name: Bypass Docker keychain (stub helper)
        run: |
          STUB_DIR="/tmp/bhl-stub-bin.$$"
          mkdir -p "$STUB_DIR"
          cat > "$STUB_DIR/docker-credential-desktop" <<'STUB'
          #!/bin/bash
          case "$1" in
            get) echo '{"ServerURL":"","Username":"","Secret":""}' ;;
            list) echo '{}' ;;
            *) echo '{}' ;;
          esac
          exit 0
          STUB
          chmod +x "$STUB_DIR/docker-credential-desktop"
          echo "STUB_DIR=$STUB_DIR" >> $GITHUB_ENV

      - name: Build Docker images
        env:
          BUILD_COMMIT_SHA: ${{ github.sha }}
          BUILD_BRANCH: ${{ github.ref_name }}
        run: |
          export PATH="$STUB_DIR:$PATH"
          export BUILD_TIME="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          docker compose -f $PROJECT_DIR/docker-compose.prod.yml build web api vrp
          echo "Build xong"
          rm -rf "$STUB_DIR" || true

      - name: Ensure SSL certs exist
        run: |
          PERSISTENT_SSL="/Users/tungtranle/Projects/Beer-HL/bhl-oms/nginx/ssl"
          WORKSPACE_SSL="$GITHUB_WORKSPACE/$PROJECT_DIR/nginx/ssl"
          mkdir -p "$WORKSPACE_SSL"
          if [ -f "$PERSISTENT_SSL/fullchain.pem" ] && [ -f "$PERSISTENT_SSL/privkey.pem" ]; then
            cp "$PERSISTENT_SSL/fullchain.pem" "$WORKSPACE_SSL/fullchain.pem"
            cp "$PERSISTENT_SSL/privkey.pem" "$WORKSPACE_SSL/privkey.pem"
            echo "SSL certs copied from persistent location (real Let's Encrypt certs)"
            openssl x509 -in "$WORKSPACE_SSL/fullchain.pem" -noout -enddate 2>/dev/null || true
          elif [ ! -f "$WORKSPACE_SSL/fullchain.pem" ] || [ ! -f "$WORKSPACE_SSL/privkey.pem" ]; then
            echo "WARNING: No real certs found at $PERSISTENT_SSL, generating self-signed"
            openssl req -x509 -nodes -days 3650 -newkey rsa:2048 \
              -keyout "$WORKSPACE_SSL/privkey.pem" \
              -out "$WORKSPACE_SSL/fullchain.pem" \
              -subj '/C=VN/ST=QuangNinh/L=HaLong/O=BHL/CN=bhl.symper.us'
            echo "Self-signed cert generated (fallback)"
          else
            echo "SSL certs already in workspace, using existing"
          fi

      - name: Restart services
        run: |
          # Single up -d: Docker Compose handles dependency order internally
          # pull_policy: if_not_present on OSRM prevents blocking pull
          docker compose -f $PROJECT_DIR/docker-compose.prod.yml up -d

      - name: Sync DB (migrations + master data)
        run: |
          sleep 8
          bash $PROJECT_DIR/scripts/db-sync.sh

      - name: Cleanup old images
        run: docker image prune -f

      - name: Health Check
        run: |
          echo "=== Nginx container status (initial) ==="
          docker compose -f $PROJECT_DIR/docker-compose.prod.yml ps nginx

          sleep 20
          for i in 1 2 3 4 5 6 7 8; do
            if curl -sf http://localhost:8080/health > /dev/null 2>&1; then
              echo "API healthy - kiem tra nginx + HTTPS..."
              docker compose -f $PROJECT_DIR/docker-compose.prod.yml ps
              HTTP_CODE=$(curl -sk -o /dev/null -w "%{http_code}" https://bhl.symper.us/health --max-time 15 2>/dev/null || echo "0")
              echo "Public HTTPS /health response: $HTTP_CODE"
              if [ "$HTTP_CODE" = "200" ]; then
                echo "=== DEPLOY THANH CONG - API + Nginx + HTTPS ==="
                exit 0
              else
                echo "=== API OK nhung HTTPS khong hoat dong (code: $HTTP_CODE) ==="
                echo "--- Nginx logs ---"
                docker compose -f $PROJECT_DIR/docker-compose.prod.yml logs nginx --tail 30
                exit 1
              fi
            fi
            echo "Thu $i/8 - doi them 10s... ($(date '+%H:%M:%S'))"
            sleep 10
          done
          echo "=== API khong phan hoi sau 100s ==="
          docker compose -f $PROJECT_DIR/docker-compose.prod.yml ps
          echo "--- API logs (last 50) ---"
          docker compose -f $PROJECT_DIR/docker-compose.prod.yml logs api --tail 50
          echo "--- Nginx logs (last 30) ---"
          docker compose -f $PROJECT_DIR/docker-compose.prod.yml logs nginx --tail 30
          exit 1
